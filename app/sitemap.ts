import type { MetadataRoute } from "next";import { products } from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const b=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000",now=new Date();const pages:MetadataRoute.Sitemap=["","/san-pham","/gio-hang","/tai-khoan"].map(path=>({url:b+path,lastModified:now,changeFrequency:path?"weekly":"daily",priority:path?0.8:1}));return pages.concat(products.map(p=>({url:`${b}/san-pham/${p.slug}`,lastModified:now,changeFrequency:"weekly" as const,priority:.7})))}
