import type { Metadata } from "next";import Image from "next/image";import Link from "next/link";import { notFound } from "next/navigation";import { Check,ShieldCheck,Star,Truck } from "lucide-react";import { getProduct,money,products } from "@/lib/data";import { AddToCart } from "@/components/add-to-cart";import { ProductCard } from "@/components/product-card";
export async function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const p=getProduct((await params).slug);return p?{title:p.name,description:p.description,alternates:{canonical:`/san-pham/${p.slug}`},openGraph:{images:[p.image]}}:{title:"Không tìm thấy sản phẩm"}}
export default async function Detail({params}:{params:Promise<{slug:string}>}){const p=getProduct((await params).slug);if(!p)notFound();const schema={"@context":"https://schema.org","@type":"Product",name:p.name,image:p.image,description:p.description,sku:p.id,offers:{"@type":"Offer",price:p.price,priceCurrency:"VND",availability:"https://schema.org/InStock"},aggregateRating:{"@type":"AggregateRating",ratingValue:p.rating,reviewCount:p.reviews}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="section"><div className="container"><div className="muted" style={{fontSize:13,marginBottom:24}}><Link href="/">Trang chủ</Link> / <Link href="/san-pham">Sản phẩm</Link> / {p.name}</div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:50}}><div style={{position:"relative",aspectRatio:"1/1",borderRadius:24,overflow:"hidden",background:"#f5f8f7"}}><Image src={p.image} alt={p.name} fill priority sizes="(max-width:760px) 100vw,50vw" style={{objectFit:"cover"}}/></div><div><span className="pill">{p.badge||p.category}</span><h1 style={{fontSize:"clamp(2rem,4vw,3rem)",lineHeight:1.15,letterSpacing:"-.04em",margin:"14px 0"}}>{p.name}</h1><div style={{display:"flex",gap:8,color:"#b45309"}}><Star size={18} fill="currentColor"/> {p.rating} <span className="muted">· {p.reviews} đánh giá</span></div><div style={{display:"flex",gap:12,alignItems:"center",margin:"22px 0"}}><strong style={{fontSize:30,color:"#087f5b"}}>{money(p.price)}</strong>{p.compareAt&&<del className="muted">{money(p.compareAt)}</del>}</div><p className="muted" style={{fontSize:17,lineHeight:1.75}}>{p.description}</p><ul style={{listStyle:"none",padding:0,display:"grid",gap:10}}><li><Check size={17} color="#087f5b"/> Còn hàng, giao ngay</li><li><Check size={17} color="#087f5b"/> Kiểm tra hàng trước khi nhận</li></ul><AddToCart product={p}/><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:10,marginTop:18,fontSize:13}}><span className="card" style={{padding:12}}><Truck size={18}/> Giao nhanh</span><span className="card" style={{padding:12}}><ShieldCheck size={18}/> Chính hãng</span><span className="card" style={{padding:12}}><Check size={18}/> Đổi 7 ngày</span></div></div></div><div style={{marginTop:54,display:"grid",gridTemplateColumns:"1fr 1fr",gap:38}}><div className="prose"><h2>Chi tiết sản phẩm</h2><p>{p.description} Thiết kế tối giản, dễ vệ sinh và phù hợp với nhịp sống của gia đình hiện đại.</p><p>Sản phẩm được kiểm tra trước khi đóng gói, đi kèm hướng dẫn sử dụng tiếng Việt và chính sách hỗ trợ rõ ràng.</p></div><div><h2>Thông số kỹ thuật</h2>{Object.entries(p.specs).map(([k,v])=><div key={k} style={{display:"flex",justifyContent:"space-between",padding:"13px 0",borderBottom:"1px solid #e4ece9"}}><span className="muted">{k}</span><b>{v}</b></div>)}</div></div></div></section><section className="section" style={{background:"#f6faf8"}}><div className="container"><h2 className="section-title">Có thể bạn cũng thích</h2><div className="grid-products">{products.filter(x=>x.id!==p.id).slice(0,4).map(x=><ProductCard key={x.id} product={x}/>)}</div></div></section><style>{`li{display:flex;gap:8px;align-items:center}@media(max-width:760px){main section .container>div[style*="1fr 1fr"]{grid-template-columns:1fr!important}}`}</style></>}
